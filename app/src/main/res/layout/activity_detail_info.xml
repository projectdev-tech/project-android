<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Toolbar -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@android:color/white"
            android:elevation="4dp" />

        <!-- Placeholder Image -->
        <ImageView
            android:id="@+id/infoImage"
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:layout_marginTop="16dp"
            android:scaleType="centerInside"
            android:src="@drawable/dummy_info_detail"
        android:background="#F2F2F2" />

        <!-- Judul Info -->
        <TextView
            android:id="@+id/textJudul"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Judul Info"
            android:textColor="#000000"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Tanggal dan Waktu -->
        <TextView
            android:id="@+id/textTanggal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="12:00:00, 24 Apr 2025"
            android:textColor="#888888"
            android:textSize="14sp" />

        <!-- Isi Info -->
        <TextView
            android:id="@+id/textIsiInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Perhatian! Aplikasi akan dalam mode pemeliharaan pada 25 Apr 2025 pukul 00:00:00 - 02:00:00 WIB. Beberapa fitur mungkin tidak tersedia selama periode ini. Anda dapat mencoba kembali setelah pemeliharaan selesai. 🛠️"
            android:textColor="#333333"
            android:textSize="15sp" />
    </LinearLayout>
</ScrollView>
