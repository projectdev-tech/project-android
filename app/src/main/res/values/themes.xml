<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProjectNew" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Ubah warna status bar jadi putih -->
        <item name="android:statusBarColor">@android:color/white</item>

        <!-- Aktifkan ikon status bar berwarna gelap -->
        <item name="android:windowLightStatusBar">true</item>

        <!-- (Opsional) Tambahan kalau kamu ingin ubah warna utama -->
        <!--
        <item name="colorPrimary">@color/your_primary_color</item>
        <item name="colorOnPrimary">@color/your_on_primary_color</item>
        -->
    </style>

    <style name="Theme.ProjectNew" parent="Base.Theme.ProjectNew" />
</resources>
